# 菜品详情库（dish-details）

> 由 meal-planner-skill 维护。  
> 本文件存放每道菜的**详情**：热量、所需食材、添加日期、用户评价、状态/停用原因。  
> `dish-library.md` 只存菜名；**凡是出现在 `dish-library.md` 的菜名，都应该在这里有对应条目**（否则无法生成采购清单/热量统计）。

---

## 早餐

### 早餐 - 肉类

#### {{dish_name}}

- 热量(kcal/份)：{{kcal}}
- 所需食材：{{ingredients}}
- 添加日期：{{added_date}}
- 用户评价：{{rating}}（可留空）
- 状态：启用 / 已停用 (原因)

### 早餐 - 蛋类

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 早餐 - 水果

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 早餐 - 主食

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

---

## 正餐

### 正餐 - 肉菜

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 正餐 - 素菜

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

---

## 可选扩展分类

> 仅当用户在档案中启用了对应结构时再创建以下分组。

### 正餐 - 主食

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 正餐 - 汤

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 招牌菜（周末/宴客）

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

### 加餐/零食

#### {{dish_name}}

- 热量(kcal/份)：
- 所需食材：
- 添加日期：
- 用户评价：
- 状态：启用 / 已停用 (原因)

